## 1. 3 because it prints the number of iterations 

## 2.  300 because it prints out discountedPrice which is a function-scope that is accessible anywhere 

## 3. will return 150 becuase its a function-scoped

## 4. [50, 100, 150] because now it goes through the full code and returns the arrays with the values

## 5. will cause an error since its using let, making it a blocked-scoped that is only accesible in the for-loop

## 6. same thing, it will return an error saying discountedPrice is not defined since it was declared using let inside the for-loop

## 7. will return 150, since its declared at the top, within the for-loop its value gets changed.

## 8. [50, 100, 150], it runs normally

## 9. it returns an error since i is a function-scope within the for-loop 

## 10. will return the length of prices.length which is 3


## 11. return [50, 100, 150]

## 12. A: student.name B: student["Grad Year"] C: student.greeting() D: student["Favorite Teacher"].name E: student.courseLoad[0]

## 13. A: '32' converts the int to a string B: 1 converts the string into an int C: 3 since null is 0 D: "3null" makes string cocatenation E: 4 since true corresponds to 1 F: 0 , 0 + 0 = 0 G: "3undefined" string cocatenation H: NaN since undefined becaused NaN and "3" becomes 3 

## 14. A: true, makes "2" into a int  B: false, checks character by character, '1' < '2' C: true, since == allows for type coercion D: False since === checks for value and type E: 1 == 2 which is false F: its comparing a truthy value and true which returns true

## 15. == compares types with coercion and === compares values and types with no coercion 

## 16. 21 45 5 2 

## 17. [2,4,6] because modifyArray function iterates from each element in the input array and uses the doSomething callback function and multiplies each number by 2/

## 18. 2:10:31 AM 2:10:32 AM 2:10:33 AM

## 19. 1 4 3 2 
