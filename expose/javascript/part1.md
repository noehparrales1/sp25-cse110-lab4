## 1. 20

## 2. 20

## 3. Shouldn't use var because its a function-scoped and its result can still be accessed outside the blocked-scoped in the if-statement

## 4. 20 

## 5. returns an error because let is a blocked-scope so result is not defined outisde the if-statement, making a ReferenceError

## 6. Throws an error, since were using const it assignes result to equal 0. Later in the code we tried to reassign result which threw a TypeError. 

## 7. Since there was an error thrown in line 9, the function exited and line 13 never went through



