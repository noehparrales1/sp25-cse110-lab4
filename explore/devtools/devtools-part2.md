## 1. The bug was that num1 and num2 would concatonate the given inputs so it would take them as string rather than ints

## 2. to fix the issue all we needed to do was use Number() on both num1 and num2 to convert to ints